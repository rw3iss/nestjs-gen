import { EntityRepository, Repository } from 'typeorm';
import { <%= modelClass %> } from './<%= modelDir %><%= modelFileName %>';

@EntityRepository(<%= modelClass %>)
export class <%= name %>Repository extends Repository<<%= modelClass %>> {

}