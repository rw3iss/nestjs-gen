import { EntityRepository, Repository } from 'typeorm';
import { <%= modelName %> } from './<%= modelDir %><%= modelNameLower %>.model';

@EntityRepository(<%= modelName %>)
export class <%=: name | capitalize %>Repository extends Repository<<%= modelName %>> {

}