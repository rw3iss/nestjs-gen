import { EntityBase } from 'models/EntityBase';
import { Column, Entity, Index  } from 'typeorm';

@Entity('<%= modelNameLower %>')
export class <%= modelName %> extends EntityBase {

    constructor() {
        super();
    }

}